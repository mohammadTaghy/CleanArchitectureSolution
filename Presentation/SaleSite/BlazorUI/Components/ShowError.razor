
<div class="row">
    @if (IsShow)
    {
        <div class="col-12 card card-body">
            Sorry but an error has occurred. @ChildContent
        </div>
    }
</div>

@code {

    [Parameter]
    public RenderFragment? ChildContent { get; set; }
    [Parameter]
    public bool IsShow { get; set; }
}